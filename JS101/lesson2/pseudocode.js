function findLargest(numbers) {
  let savedNumber = numbers[0];

  numbers.forEach(num => {
    if (num > savedNumber) {
      savedNumber = num;
    }
  })
}

// a function that returns the sum of two numbers
// - START
// - # Given two numbers
// - Add number one and number two together
// - ASSIGN the addition to another variable addi
// - PRINT addi
// - END

// function that take an array of strings and return concatenation of the strings
// - START
// - # Given an array of strings
// - INITIALIZE empty output string
// - FOR each string in array
// - ADD the string to the output string
// - PRINT output string
// - END

// function that takes array of integers and returns array with every other element
// - START
// - given array of integers
// - initialize an empty output array
// - FOR each element in the array
// - add first element to output array
// - IF element index is divisible evenly by 2
// - THEN add to output array
// - PRINT output array
// - END

// function that returns the index of the third occurrence of a character
// - START
// - Given the character and the string
// - split the string into individual characters and save to a variable
// - Initialize a counter variable
// - initialize a second counter variable
// - FOR each element in the resulting split string
// - IF element equals the focus element
//    - THEN add to counter
//    - IF counter equals three
      // - PRINT index variable
// - ELSE increment variable for index

// funciton that merges two arrays
// - GIven two arrays
// - intialize index varaible at 0
// - While index variable less than or equal to counter variable
// -    add element from array1 index to output array
// -    add element from array2 index to output array
// -    incremement index




